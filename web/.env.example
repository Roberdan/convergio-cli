# ============================================================================
# CONVERGIO WEB - Environment Configuration
# Copy this file to .env.local and fill in your values
# ============================================================================

# ---- REQUIRED: Azure OpenAI Realtime (for voice sessions) ----
# The web app uses Azure OpenAI Realtime API for voice interactions.
# You must have an Azure OpenAI resource with gpt-4o-realtime-preview deployed.

AZURE_OPENAI_REALTIME_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_REALTIME_API_KEY=your-api-key
AZURE_OPENAI_REALTIME_DEPLOYMENT=gpt-4o-realtime-preview

# ---- OPTIONAL: Azure OpenAI for Chat (text-based conversations) ----
# If not set, chat features will be disabled or use a fallback

# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
# AZURE_OPENAI_API_KEY=your-api-key
# AZURE_OPENAI_DEPLOYMENT=gpt-4o

# ---- OPTIONAL: Budget Control ----
# Monthly spending limit in your local currency

# CONVERGIO_BUDGET=50

# ============================================================================
# DEPLOYMENT NOTES:
#
# 1. For Vercel deployment:
#    - Add these variables in Project Settings > Environment Variables
#    - No need to upload .env.local file
#
# 2. For Docker deployment:
#    - Pass variables via --env-file or -e flags
#    - Example: docker run --env-file .env.local convergio-web
#
# 3. For local development:
#    - Copy this file: cp .env.example .env.local
#    - Fill in your Azure OpenAI credentials
#    - Run: npm run dev
# ============================================================================
